{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "add",
  "description": "Capability for the add window",
  "windows": ["add"],
  "permissions": [
    "core:window:allow-close",
    {
      "identifier": "http:default",
      "allow": [
        {
          "url": "https://**"
        },
        {
          "url": "http://**"
        }
      ]
    }
  ]
}
